[project]
name = "room-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.28.1",
    "ipykernel>=7.2.0",
]

[dependency-groups]
dev = [
    "ruff>=0.15.2",
]
